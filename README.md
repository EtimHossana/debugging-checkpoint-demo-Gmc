1. Clone the repository:
   ```bash
   git clone https://github.com/yourusername/react-debugging-sample.git
   cd react-debugging-sample
Install dependencies:

bash
Copy
Edit
npm install
Start the development server:

bash
Copy
Edit
npm start
Open the app in your browser at http://localhost:3000
